/**
 * A module exposing a {@link Promise} constructor.
 *
 * @module {Promise} $promise
 * @example Combine a number of {@link Promise} instances.
 * Esquire.define("myModule", ['$promise'], function(Promise) {
 *   // Combine a bunch of promises
 *   return Promise.all(...)
 *     .then(function(results) {
 *       // all results
 *     });
 * });
 */
